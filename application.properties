# Local application configuration
# Environment variables still override these values when set.

DATABASE_URL=

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

JWT_SECRET=
JWT_EXP_MINUTES=1440
